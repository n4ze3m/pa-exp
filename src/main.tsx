import "./index.css"
import React from "react";
import ReactDOM from "react-dom/client";
import IndexOption from "./entries/options/App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <IndexOption />
  </React.StrictMode>
);